# TuttiBot Backend - Render-Optimized Requirements
# This version uses only packages available as pre-built wheels on PyPI
# Avoids packages that require C compilation (like pyaudio)

# ============================================================================
# WEB FRAMEWORK & API
# ============================================================================
flask>=2.0.0
flask-cors>=3.0.10
gunicorn>=20.1.0
werkzeug>=2.0.0

# ============================================================================
# AI & LLM
# ============================================================================
groq>=0.4.0

# ============================================================================
# CORE SCIENTIFIC LIBRARIES
# ============================================================================
numpy>=1.21.0,<2.0
scipy>=1.7.0
scikit-learn>=1.0.0
pandas>=1.3.0

# ============================================================================
# AUDIO PROCESSING (Pre-built wheels only)
# ============================================================================
librosa>=0.9.0
soundfile>=0.10.0
noisereduce>=2.0.0
ffmpeg-normalize>=1.26.0
# auditok will pull in numpy/scipy which are already available
auditok>=0.2.1
resampy>=0.2.2
# audioread - has pre-built wheels for Linux
audioread>=2.1.9
# pydub - pure Python, no compilation needed
pydub>=0.25.0

# ============================================================================
# MUSIC PROCESSING & MIDI (Pre-built wheels)
# ============================================================================
pretty_midi>=0.2.9
music21>=7.1.0
mido>=1.2.10

# ============================================================================
# DEEP LEARNING (for Transcription & PQG-A2SA)
# ============================================================================
torch>=1.9.0
torchaudio>=0.9.0
basic-pitch>=0.2.0

# ============================================================================
# VISUALIZATION
# ============================================================================
matplotlib>=3.4.0
seaborn>=0.11.0

# ============================================================================
# UTILITIES
# ============================================================================
pyyaml>=5.4.0
tqdm>=4.62.0
joblib>=1.0.0
