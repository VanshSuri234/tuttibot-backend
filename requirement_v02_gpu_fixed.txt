# TuttiBot v02 - Temporal Alignment Pipeline Requirements (GPU-Ready) - COMPLETE VERSION
# Install with: pip install -r requirement_v02_gpu_fixed.txt
# Fixed compatibility issues for Python 3.13 and includes ALL dependencies tested
# Updated: September 3, 2025 - Includes all libraries needed for successful pipeline execution

# Core Dependencies
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0

# Audio Processing (TESTED - Required for librosa and audio handling)
librosa>=0.9.0
soundfile>=0.10.0
audioread==3.0.1  # Fixed version for Python 3.13 compatibility
soxr>=0.3.2  # Required by librosa
numba>=0.56.0  # Required by librosa and for GPU acceleration
llvmlite<0.45,>=0.44.0dev0  # Required by numba
scikit-learn>=1.1.0  # Required by librosa
joblib>=1.0  # Required by scikit-learn
decorator>=4.3.0  # Required by librosa
pooch>=1.1  # Required by librosa
lazy_loader>=0.1  # Required by librosa
msgpack>=1.0  # Required by librosa
threadpoolctl>=3.1.0  # Required by scikit-learn

# Audio format support (TESTED - Required for Python 3.13)
standard-aifc>=3.13.0  # Python 3.13 audio format support
standard-sunau>=3.13.0  # Python 3.13 audio format support
standard-chunk>=3.13.0  # Required by standard-aifc
audioop-lts>=0.2.2  # Required by standard-aifc

# Music Processing (TESTED - All working)
music21>=8.0.0
pretty_midi>=0.2.9
mido>=1.2.0
chardet>=5.2.0  # Required by music21
jsonpickle>=4.1.1  # Required by music21
more-itertools>=10.8.0  # Required by music21
webcolors>=1.5  # Required by music21

# PDF Processing (TESTED - Required for PDF score conversion)
pdf2image>=1.17.0
oemer>=0.1.8  # Optical Music Recognition
opencv-python-headless>=4.5.3.56  # Required by oemer
onnxruntime-gpu>=1.22.0  # Required by oemer (falls back to CPU if no GPU)
coloredlogs>=15.0.1  # Required by onnxruntime
humanfriendly>=10.0  # Required by coloredlogs
flatbuffers>=25.2.10  # Required by onnxruntime
protobuf>=6.32.0  # Required by onnxruntime
sympy>=1.14.0  # Required by onnxruntime
mpmath<1.4,>=1.1.0  # Required by sympy

# Type hints and development support (TESTED - Required by oemer)
types-Pillow>=10.2.0.20240822
types-tensorflow>=2.18.0.20250809
types-protobuf>=6.30.2.20250822
types-requests>=2.32.4.20250809

# TensorFlow with GPU support (Python 3.13 compatible)
tensorflow>=2.20.0  # Latest version supporting Python 3.13
absl-py>=2.3.1  # Required by TensorFlow
astunparse>=1.6.3  # Required by TensorFlow
gast!=0.5.0,!=0.5.1,!=0.5.2,>=0.2.1  # Required by TensorFlow
google-pasta>=0.2.0  # Required by TensorFlow
libclang>=18.1.1  # Required by TensorFlow
opt-einsum>=3.4.0  # Required by TensorFlow
termcolor>=3.1.0  # Required by TensorFlow
wrapt>=1.17.3  # Required by TensorFlow
grpcio<2.0,>=1.24.3  # Required by TensorFlow
tensorboard~=2.20.0  # Required by TensorFlow
keras>=3.10.0  # Required by TensorFlow
h5py>=3.14.0  # Required by TensorFlow
ml-dtypes<1.0.0,>=0.5.1  # Required by TensorFlow

# TensorBoard dependencies (TESTED - Required for TensorFlow)
markdown>=3.8.2  # Required by tensorboard
tensorboard-data-server<0.8.0,>=0.7.0  # Required by tensorboard
werkzeug>=3.1.3  # Required by tensorboard
MarkupSafe>=3.0.2  # Required by werkzeug
jinja2>=3.1.6  # Required by werkzeug

# Keras dependencies (TESTED - Required for TensorFlow)
namex>=0.1.0  # Required by keras
optree>=0.17.0  # Required by keras

# PyTorch with GPU support (alternative to TensorFlow)
torch>=1.12.0
torchaudio>=0.12.0
# PyTorch CUDA dependencies (auto-installed with torch)
nvidia-cuda-nvrtc-cu12==12.8.93  # CUDA runtime
nvidia-cuda-runtime-cu12==12.8.90  # CUDA runtime
nvidia-cuda-cupti-cu12==12.8.90  # CUDA profiling
nvidia-cudnn-cu12==9.10.2.21  # cuDNN
nvidia-cublas-cu12==12.8.4.1  # CUDA BLAS
nvidia-cufft-cu12==11.3.3.83  # CUDA FFT
nvidia-curand-cu12==10.3.9.90  # CUDA random
nvidia-cusolver-cu12==11.7.3.90  # CUDA solver
nvidia-cusparse-cu12==12.5.8.93  # CUDA sparse
nvidia-cusparselt-cu12==0.7.1  # CUDA sparse LT
nvidia-nccl-cu12==2.27.3  # NVIDIA collective
nvidia-nvtx-cu12==12.8.90  # NVIDIA tools
nvidia-nvjitlink-cu12==12.8.93  # NVIDIA JIT
nvidia-cufile-cu12==1.13.1.3  # CUDA file
triton==3.4.0  # Required by PyTorch

# PyTorch dependencies (TESTED)
filelock>=3.19.1  # Required by torch
networkx>=3.5  # Required by torch
fsspec>=2025.9.0  # Required by torch

# Visualization and Analysis (TESTED - All working)
matplotlib>=3.5.0
seaborn>=0.11.0
contourpy>=1.3.3  # Required by matplotlib
cycler>=0.12.1  # Required by matplotlib
fonttools>=4.59.2  # Required by matplotlib
kiwisolver>=1.4.9  # Required by matplotlib
pillow>=11.3.0  # Required by matplotlib
pyparsing>=3.2.3  # Required by matplotlib

# System monitoring and GPU management (TESTED - Required)
psutil>=5.8.0

# Standard library backports and utilities (TESTED)
tqdm>=4.62.0
six>=1.17.0  # Required by many packages
python-dateutil>=2.9.0.post0  # Required by pandas
pytz>=2025.2  # Required by pandas
tzdata>=2025.2  # Required by pandas
setuptools>=65.0.0  # Ensure modern setuptools for compatibility

# HTTP and networking (TESTED - Required by multiple packages)
requests>=2.32.3  # Required by multiple packages
charset-normalizer<4,>=2  # Required by requests
idna<4,>=2.5  # Required by requests
urllib3<3,>=1.21.1  # Required by requests
certifi>=2017.4.17  # Required by requests

# System compatibility
packaging>=24.2  # Required by many packages
typing-extensions>=4.12.2  # Required by many packages
cffi>=1.17.1  # Required by soundfile
pycparser>=2.21  # Required by cffi
platformdirs>=4.3.7  # Required by pooch

# Development and rich output (TESTED - Required by keras)
rich>=13.9.4  # Required by keras for rich output
markdown-it-py>=2.2.0  # Required by rich
pygments<3.0.0,>=2.13.0  # Required by rich
mdurl~=0.1  # Required by markdown-it-py

# Alternative AMT libraries (OPTIONAL - for advanced users)
# basic-pitch>=0.4.0  # Commented out due to TensorFlow version conflicts
# mir-eval>=0.6  # For music information retrieval evaluation
# madmom>=0.16.1  # Alternative audio processing library

# IMPORTANT NOTES:
# ================
# 1. This requirements file includes ALL dependencies needed for successful pipeline execution
# 2. Tested on Python 3.13.5 with all pipeline blocks working except basic-pitch
# 3. PDF processing (oemer) works for score conversion
# 4. TensorFlow 2.20.0 is compatible with Python 3.13
# 5. All CUDA dependencies are included for GPU support (falls back to CPU automatically)
# 6. For basic-pitch compatibility, use Python 3.11 environment instead
# 7. All packages have been tested and verified working together

# INSTALLATION:
# =============
# pip install -r requirement_v02_gpu_fixed.txt

# VERIFICATION:
# =============
# python -c "import tensorflow as tf; import torch; import librosa; import music21; print('✅ All packages imported successfully')"

# IMPORTANT: If you encounter basic-pitch issues, you can:
# 1. Use Python 3.11 instead of 3.13
# 2. Use alternative AMT methods (madmom, librosa onset detection)
# 3. Implement custom AMT using PyTorch/TensorFlow directly
