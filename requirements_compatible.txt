# TuttiBot v02 - Compatible Requirements
# Python 3.12 Compatible (3.10-3.12 supported)
# 
# Installation Instructions:
# 1. Create virtual environment: python3.12 -m venv tuttibot_env
# 2. Activate: source tuttibot_env/bin/activate  
# 3. Upgrade pip: pip install --upgrade pip
# 4. Install requirements: pip install -r requirements_compatible.txt
#
# Key Compatibility Notes:
# - TensorFlow 2.12 with NumPy 1.23.x for GPU support
# - Basic Pitch 0.3.0 for stable audio transcription
# - ONNX Runtime for PDF conversion compatibility

# Core ML Stack - TensorFlow (Python 3.12 compatible)
# Note: Keras is included in TensorFlow 2.16+
tensorflow>=2.16.0,<2.20.0

# NumPy - Compatible with newer TensorFlow
numpy>=1.24.0,<2.0.0

# Audio ML - Basic Pitch and dependencies (updated versions)
basic-pitch>=0.4.0
librosa>=0.10.0
mir-eval>=0.7.0
pretty-midi>=0.2.10
resampy>=0.4.0
soundfile>=0.12.1

# Scientific Computing (updated)
scipy>=1.10.0
scikit-learn>=1.3.0

# PDF Processing (updated)
onnxruntime>=1.15.0
opencv-python>=4.8.0

# Music Processing (updated)
music21>=9.0.0

# Utilities (updated)
matplotlib>=3.7.0
pandas>=2.0.0
Pillow>=10.0.0
tqdm>=4.65.0

# Audio codecs
ffmpeg-python>=0.2.0

# Core dependencies for modern Python/TensorFlow
protobuf>=4.21.0
absl-py>=1.4.0
