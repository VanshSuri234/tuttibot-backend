# TuttiBot v02 - Temporal Alignment Pipeline Requirements (GPU-Ready)
# Install with: pip install -r requirement_v02.txt

# Core Dependencies
numpy>=1.21.0,<2.0.0
scipy>=1.7.0
pandas>=1.3.0

# Audio Processing
librosa>=0.9.0
soundfile>=0.10.0
audioread>=2.1.0

# Music Processing
music21>=8.0.0
pretty_midi>=0.2.9
mido>=1.2.0

# Deep Learning (with version constraints for compatibility)
tensorflow>=2.10.0,<2.15.0  # Compatible with basic-pitch
torch>=1.12.0
torchvision>=0.13.0

# Basic Pitch for AMT (Block 1) - Fixed version for compatibility
basic-pitch>=0.2.4,<0.4.0

# Enhanced Block 2 Dependencies
matplotlib>=3.5.0
seaborn>=0.11.0

# System monitoring and GPU management
psutil>=5.8.0

# Utilities
tqdm>=4.62.0
pathlib>=1.0.0
argparse>=1.4.0

# Additional dependencies for GPU-ready version
resampy>=0.2.2
mir_eval>=0.6

# Development Dependencies (optional)
# pytest>=6.0.0
# jupyter>=1.0.0

# Installation Notes:
# - For GPU support: Ensure CUDA drivers are installed
# - For HPC systems: May need to load CUDA modules first
# - Version constraints ensure compatibility between packages