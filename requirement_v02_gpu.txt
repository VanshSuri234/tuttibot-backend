# TuttiBot v02 - Temporal Alignment Pipeline Requirements (GPU-Ready)
# Install with: pip install -r requirement_v02_gpu.txt

# Core Dependencies
numpy>=1.21.0,<2.0.0  # Compatible with TensorFlow and opencv
scipy>=1.7.0
pandas>=1.3.0

# Audio Processing
librosa>=0.9.0
soundfile>=0.10.0
audioread>=2.1.0

# Music Processing
music21>=8.0.0
pretty_midi>=0.2.9
mido>=1.2.0

# Basic Pitch for AMT (Block 1) - with GPU support
basic-pitch>=0.4.0

# TensorFlow with GPU support (automatic CPU fallback)
tensorflow>=2.8.0

# PyTorch with GPU support (automatic CPU fallback)
torch>=1.12.0
torchaudio>=0.12.0

# Enhanced Block 2 Dependencies
matplotlib>=3.5.0
seaborn>=0.11.0

# System monitoring and GPU management
psutil>=5.8.0

# Utilities
tqdm>=4.62.0
pathlib>=1.0.0
argparse>=1.4.0

# Development Dependencies (optional)
# pytest>=6.0.0
# jupyter>=1.0.0

# PDF Processing (for direct PDF score input)
pdf2image>=1.16.0  # PDF to image conversion
oemer>=0.1.8       # Optical Music Recognition (OMR)

# GPU-specific optimizations (optional but recommended)
# CUDA-accelerated libraries (will use CPU versions if CUDA not available)
numba>=0.56.0  # For GPU-accelerated numerical computations

# Notes:
# - TensorFlow and PyTorch will automatically detect and use GPU if available
# - CPU versions will be used as fallback if no GPU is detected
# - All libraries are compatible with both local systems and HPC environments
# - For SLURM/HPC: ensure CUDA modules are loaded before installation
# - For PDF processing: requires poppler-utils (install: sudo apt install poppler-utils)
# - Recommended: Use Python 3.11 for best compatibility with all dependencies
