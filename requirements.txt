# Music Performance Analysis Pipeline - Requirements
# Python 3.10 or higher required
# Optimized for Render deployment

# ============================================================================
# IMPORTANT: Dependency Exclusion
# ============================================================================
# Some packages have optional dependencies we don't need:
# - auditok tries to install pyaudio (microphone recording)
# - We use auditok only for audio segmentation, not microphone input
# Using --prefer-binary ensures we get pre-built wheels when available
#
# Install with: pip install --prefer-binary -r requirements.txt

# ============================================================================
# WEB FRAMEWORK & API
# ============================================================================
flask>=2.0.0
flask-cors>=3.0.10
gunicorn>=20.1.0
werkzeug>=2.0.0

# ============================================================================
# AI & LLM
# ============================================================================
groq>=0.4.0

# ============================================================================
# CORE SCIENTIFIC LIBRARIES
# ============================================================================
# Careful with numpy version to ensure compatibility with other libraries
numpy>=1.21.0,<2.0
scipy>=1.7.0
scikit-learn>=1.0.0
pandas>=1.3.0

# ============================================================================
# AUDIO PROCESSING
# All these packages are available as pre-built wheels on PyPI
# ============================================================================
librosa>=0.9.0
soundfile>=0.10.0
noisereduce>=2.0.0
ffmpeg-normalize>=1.26.0
auditok>=0.2.1
resampy>=0.2.2
audioread>=2.1.9
pydub>=0.25.0

# ============================================================================
# MUSIC PROCESSING & MIDI
# ============================================================================
pretty_midi>=0.2.9
music21>=7.1.0
mido>=1.2.10

# ============================================================================
# DEEP LEARNING (for Transcription & PQG-A2SA)
# ============================================================================
torch>=1.9.0
torchaudio>=0.9.0
basic-pitch>=0.2.0

# Note: For CUDA support (GPU acceleration), install PyTorch separately:
# Visit: https://pytorch.org/get-started/locally/
# Example for CUDA 11.8:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118

# ============================================================================
# BEAT DETECTION (optional)
# ============================================================================
# beatnet - Install from source if needed:
# pip install git+https://github.com/mjhydri/BeatNet.git

# ============================================================================
# VISUALIZATION
# ============================================================================
matplotlib>=3.4.0
seaborn>=0.11.0

# ============================================================================
# UTILITIES
# ============================================================================
pyyaml>=5.4.0
tqdm>=4.62.0
joblib>=1.0.0

# ============================================================================
# INSTALLATION NOTES
# ============================================================================
# This requirements file uses only pre-built wheel distributions
# to ensure compatibility with Render's build environment.
# No C compilation is required during pip install.
#
# CPU-only installation:
#   pip install --prefer-binary -r requirements.txt
#
# GPU installation (recommended):
#   1. Install PyTorch with CUDA from pytorch.org
#   2. pip install --prefer-binary -r requirements.txt
#
# ============================================================================
# SYSTEM DEPENDENCIES (pre-installed on Render)
# ============================================================================
# Render provides:
#   - libsndfile1 (audio file support)
#   - ffmpeg (audio/video processing)
#   - libportaudio2 (audio I/O - if needed)
#   - Python build tools (gcc, etc. - for build phase only)
