Python 3.10 or higher

Core Dependencies:
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.0.0
pandas>=1.3.0

Audio Processing:
librosa>=0.9.0
soundfile>=0.10.0
resampy>=0.2.2
audioread>=2.1.9

Music Processing:
pretty_midi>=0.2.9
music21>=7.1.0
mido>=1.2.10

Deep Learning (for AMT and PQG-A2SA):
torch>=1.9.0
torchaudio>=0.9.0
tensorflow>=2.6.0
basic-pitch>=0.2.0

Visualization:
matplotlib>=3.4.0
seaborn>=0.11.0

Utilities:
pyyaml>=5.4.0
tqdm>=4.62.0
joblib>=1.0.0

Optional Dependencies:
beatnet (for beat detection, may not be in PyPI, install from source if needed)

Installation:

Standard install:
pip install numpy scipy scikit-learn pandas librosa soundfile resampy audioread pretty_midi music21 mido torch torchaudio tensorflow basic-pitch matplotlib seaborn pyyaml tqdm joblib

For CUDA support (recommended):
Install PyTorch with CUDA from https://pytorch.org/get-started/locally/
Then install other packages

Note: Some dependencies may require system libraries like libsndfile, ffmpeg
